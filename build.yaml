targets:
  $default:
    builders:
      drift_dev:
        options:
          store_date_time_values_as_text: true
          named_parameters: true

      riverpod_generator:
        options:
          provider_family_name: family
